root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/gateway ./cmd/gateway"
  bin = "./tmp/gateway"
  include_ext = ["go", "yaml"]
  exclude_dir = ["gen", ".git", "tmp", ".devspace", ".openapi", "charts", "dist"]
  delay = 1000

[log]
  time = false
